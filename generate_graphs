import numpy as np
import matplotlib.pyplot as plt
import matplotlib.dates as mdates
import datetime as dt
import pprint
import csv




def showPlot(labels):
    data = np.genfromtxt('stats/commits_by_author.dat')
    dates=[dt.datetime.fromtimestamp(ts) for ts in data[0:,0]]

    dates=[dt.datetime.fromtimestamp(ts) for ts in data[0:,0]]

    plt.gca().xaxis.set_major_formatter(mdates.DateFormatter('%d.%m.%Y'))

    legend = []
    for i in range(1, data[0].size):
        legend += plt.plot(dates, data[0:,i])

    plt.legend(names[0:], loc=2)
    plt.grid(True)

    plt.savefig('test.png')

    plt.show()


names=['Claas', 'Anna Lena', 'Soeren', 'Simon', 'Paul', 'Stephanie', 'Anna', 'Vithujah', 'Yannick Broeker', 'Viktor Richter', 'Michael Goetting']
showPlot(names)
